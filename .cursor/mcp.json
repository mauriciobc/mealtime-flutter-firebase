{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "experimental:mcp",
        "--dir", "/home/mauriciobc/Documentos/Code/mealtime-flutter-firebase",
        "--only", "auth,firestore,storage"
      ]
    }
  }
}
